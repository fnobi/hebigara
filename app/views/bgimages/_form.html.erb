<%= form_for(@bgimage) do |f| %>
  <% if @bgimage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bgimage.errors.count, "error") %> prohibited this bgimage from being saved:</h2>

      <ul>
      <% @bgimage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :path %><br />
    <%= f.text_field :path %>
  </div>
  <div class="field">
    <%= f.label :fixed %><br />
    <%= f.check_box :fixed %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
