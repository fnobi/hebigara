<%= form_for(@snake) do |f| %>
  <% if @snake.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@snake.errors.count, "error") %> prohibited this snake from being saved:</h2>

      <ul>
      <% @snake.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
